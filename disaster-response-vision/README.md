# disaster-response-vision

Installable Python package for the Disaster Response Vision project.

See the [repository README](../README.md) for documentation, quickstart, and architecture.

```powershell
pip install -e ".[dev]"
disaster-vision detect data/samples --no-alert
disaster-vision-api
```
